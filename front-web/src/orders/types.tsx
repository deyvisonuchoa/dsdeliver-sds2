export type Product = {
    id: number,
    name: string,
    price: number,
    description: string,
    image_Uri: string
}

export type OrderLocationdata = {
    address: string,
    latitude: number,
    longitude: number
}